#login
  - if @current_user
    %div#main.row
    
    %p.col-sm-4  Welcome, #{@current_user.name}
    %p.col-sm-4
    = link_to 'Log Out', logout_path, :method => :post,:class => 'col-sm-1'
    
  - else
  
    %p.form-signin-heading= link_to 'Log in with your Twitter account', '/auth/twitter'